[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thinkingsdk"
version = "0.1.4"
description = "AI crash debugging for Python in production"
readme = "README.md"
authors = [
    {name = "srikar appalaraju", email = "srikar2097@gmail.com"}
]
license = {text = "MIT"}
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Debuggers",
    "Topic :: System :: Monitoring",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]
keywords = ["crash reporting", "error tracking", "exceptions", "debugging", "observability", "monitoring", "AI", "production"]
requires-python = ">=3.7"
dependencies = [
    "requests>=2.25.0",
    "pyyaml>=5.4",
    # Imported unconditionally at module scope by enhanced_context and performance_monitor, both of
    # which sit on the import path of `import thinkingsdk`. Undeclared, a clean install produced a
    # package that could not be imported at all. Declared rather than guarded because process and
    # memory sampling is core to what this SDK does, not an optional integration. See issue #14.
    "psutil>=5.9",
    "contextvars>=2.4;python_version<'3.7'",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
    "black>=22.0",
    "flake8>=5.0",
    "mypy>=1.0",
    "psutil>=5.9",
]
performance = [
    "psutil>=5.9",
]
keyring = [
    "keyring>=24.0",
]

[project.entry-points.pyinstaller40]
# Lets PyInstaller discover the hooks in thinkingsdk/__pyinstaller automatically, so consumers do
# not need --exclude-module for every optional integration. See issue #12.
hook-dirs = "thinkingsdk.__pyinstaller:get_hook_dirs"

[project.urls]
Homepage = "https://github.com/srikarappal/thinkingsdk-python"
Website = "https://thinkingsdk.ai"
Issues = "https://github.com/srikarappal/thinkingsdk-python/issues"

[project.scripts]
thinkingsdk = "thinkingsdk.validate:main"

[tool.setuptools.packages.find]
include = ["thinkingsdk*"]
namespaces = false

[tool.setuptools.package-data]
thinkingsdk = ["*.yaml", "*.yml"]

[tool.black]
line-length = 100
target-version = ['py37', 'py38', 'py39', 'py310', 'py311']

[tool.mypy]
python_version = "3.7"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["thinkingsdk"]
omit = ["*/tests/*", "*/test_*.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.:",
    "raise AssertionError",
    "raise NotImplementedError",
]